#include "headers.h"
#include "declarations.h"
Node* creatLinkList(Node* start)
{
	printf("%s : Begin\n", __func__);
	if(!start)
                return (*ctNd)(start);
        else
        {
                return start;
        }
        return start;
	printf("%s : End\n", __func__);
}

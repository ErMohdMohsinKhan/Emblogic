#include "headers.h"
#include "dataStructures.h"
#include "declarations.h"

int isOperator(char a)
{
//      printf("%s : Begin\n, stktop : %d\n", __func__, stk->top);
	if(a == '+'|| a == '-' || a == '*' || a == '/')
        {
                return 1;
        }
}


#include "headers.h"
#include "class.h"

Counter Counter::operator++()
{
        cout<<"Begin Operator Overloading Function"<<endl;
        int temp;  //make a temporary Counter
        ++count;
        temp = count;

        cout<<"End Operator Overloading Function"<<endl;
        return (Counter)temp;
}

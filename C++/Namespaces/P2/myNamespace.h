namespace circle
{
	const double PI = 3.14159;
	double circumf(double radius)
	{
	       	return (2 * PI * radius);
	}
}
namespace circle
{
	double area(double radius)
	{
	       	return (PI * radius * radius);
	}
}
